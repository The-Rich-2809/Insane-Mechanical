@{
    Layout = "_LayoutV";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Verification Failed</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        .modal-content {
            border-radius: 10px;
        }

        .crossmark {
            width: 50px;
            height: 50px;
            margin: 0 auto;
        }

        .crossmark-line {
            stroke-dasharray: 75;
            stroke-dashoffset: 75;
            stroke-width: 4;
            stroke-miterlimit: 10;
            stroke: #e53e3e;
            fill: none;
            animation: stroke 0.6s cubic-bezier(0.65, 0, 0.45, 1) forwards;
        }

        @@keyframes stroke {
            100% {
                stroke-dashoffset: 0;
            }
        }
    </style>
</head>
<body>
    <!-- Modal -->
    <div class="modal fade" id="verificationFailedModal" tabindex="-1" aria-labelledby="verificationFailedModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-body text-center">
                    <div class="crossmark">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
                            <circle class="crossmark-circle" cx="26" cy="26" r="25" fill="none" />
                            <path class="crossmark-line" fill="none" stroke="#e53e3e" stroke-width="4" d="M16 16 36 36 M36 16 16 36" />
                        </svg>
                    </div>
                    <h2 class="mt-3">Verificación Fallida</h2>
                    <p>Tu código de verificación es incorrecto o ha expirado. Por favor inténtalo nuevamente.</p>
                    <a href="@Url.Action("Login", "Home")" class="btn btn-primary mt-3">Volver al login</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#verificationFailedModal').modal('show');
        });
    </script>
</body>
</html>
